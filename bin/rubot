#!/usr/bin/env ruby

require "bundler/setup"
require "value_caster"

# monkey patch
module Slack
  class Client
    def reactions_list(options = {})
      post('reactions.list', options)
    end
  end
end

ValueCaster::Bot.new(token: ENV['SLACK_BOT_API_TOKEN']).run
